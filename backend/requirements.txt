# 🚨 08/09/2026 — DUAS PORTAS PÚBLICAS ABERTAS POR DEPENDÊNCIA PARADA.
# Conferido por mim na API do OSV (api.osv.dev), não em documento:
#
#   starlette 0.38.6 (o que o fastapi 0.115.0 ARRASTAVA) .... 14 vulns, 3 ALTAS
#   python-multipart 0.0.18 ................................. 14 vulns, 3 ALTAS
#
# O caminho: `POST /api/estimate-price` é PÚBLICO (sem login). As três travas
# que existem — rate limit por IP, teto de 450 MB e o cheque de JWT — moram
# DENTRO do corpo da rota, e o FastAPI só chega lá DEPOIS de já ter parseado o
# formulário. Com 4 GB e `--workers 1`, poucas requisições simultâneas derrubam
# o backend pra TODOS os clientes, não só pro atacante.
#
# 🪤 SUBIR SÓ PRA starlette 0.40.0 (o mínimo que fecha a CVE citada na
# auditoria) fecharia UMA das três altas. As outras duas só caem em 1.1.0 e
# 1.3.1 — e o `fastapi==0.115.0` PROÍBE isso: o pin dele é `starlette<0.39.0`.
# Por isso o fastapi sobe junto: do 0.136.1 em diante o pin vira
# `starlette>=0.46.0`, sem teto, e o starlette moderno passa a ser possível.
#
# 🔑 O pin do starlette fica EXPLÍCITO aqui de propósito. Ele era transitivo, e
# dependência transitiva sem trava é exatamente o que ficou dois anos parada
# sem ninguém ver.
#
# 📏 O 0.136.1 não é chute: é a versão contra a qual esta bancada já roda
# (2.9k testes) na máquina onde ela foi escrita. A produção é que estava atrás.
# 🪤 E isso é um achado em si: a bancada vinha validando bibliotecas que a
# produção NÃO tinha. Ver [[feedback_medir_com_ferramenta_que_a_producao_nao_tem]].
fastapi==0.136.1
starlette==1.3.1
uvicorn==0.30.0
python-multipart==0.0.31
# 🚨 08/09/2026 — o pdfplumber PINA o pdfminer.six, e o 0.11.0 prendia a versão
# 20231228: duas falhas ALTAS de EXECUÇÃO DE CÓDIGO por desserialização de
# pickle (GHSA-f83h e GHSA-wf5f), conferidas por mim no OSV.
# 🪤 NÃO adianta pôr `pdfminer.six>=...` aqui sem subir o pdfplumber: dá
# conflito de resolução, porque o pin dele é `==`.
#
# 🩸 E EU CAÍ NO IRMÃO DESSE MESMO BURACO, UM NÍVEL ACIMA. Pinei 0.11.10 e
# derrubei o CI e o build do Render de uma vez — os dois no passo de INSTALAR,
# não em teste:
#     pdfplumber 0.11.10 depends on pypdfium2>=5.9.0
#     The user requested pypdfium2==4.30.0
# O pdfplumber prende o `pypdfium2` do mesmo jeito que prende o pdfminer, e o
# `pypdfium2` é o RASTERIZADOR do motor — a versão 5 seria salto maior no
# coração do PDF, não um patch de segurança.
#
# ✅ 0.11.9 é o degrau que resolve: `pypdfium2>=4.18.0` (o nosso 4.30.0 serve)
# e já pina `pdfminer.six==20251230`. Conferido no OSV, com controle:
# o 20231228 tem 4 falhas (2 ALTAS), o 20251230 tem ZERO. A segurança inteira,
# sem encostar no rasterizador.
#
# 📏 Alcance honesto: o mantenedor do pdfminer diz que, em Linux, o PDF sozinho
# NÃO dispara — precisa de um pickle já no disco. Subimos porque é barato — e
# quando a versão 0.11.10 provou NÃO ser barata, ela caiu por esse motivo.
pdfplumber==0.11.9
pypdfium2==4.30.0
openpyxl==3.1.5
python-docx==1.1.2
anthropic==0.40.0
# 08/09/2026 — 13 falhas ALTAS no 10.4.0 (escrita fora de limite no heap,
# bombas de descompressao), conferidas no OSV.
# 📏 ALCANCE HONESTO, e por isso nao e alta pra nos: o Pillow aqui recebe um
# bitmap JA DECODIFICADO pelo pypdfium2 (`bitmap.to_pil()`), nao o PDF do
# cliente — e as falhas exigem que ele PARSEIE formatos que a gente nao usa
# (PSD, FITS, BDF, PCF, GD, JPEG2000, McIdas). Subimos porque e barato: a
# bancada ja rodava contra o 12.x na maquina onde ela e escrita.
Pillow==12.3.0
python-dotenv==1.0.1
stripe==10.0.0
httpx==0.27.0
ezdxf==1.4.3
matplotlib==3.9.2
psutil==5.9.8
reportlab==4.2.5
python-pptx==1.0.2
shapely==2.1.2
pikepdf==10.9.1
# 🚫 08/09/2026 — FICA NO 62.3, E E DECISAO, NAO ESQUECIMENTO.
# A unica falha ALTA dele (GHSA-983w, fix 68.0) e um BYPASS de protecao de
# SSRF por redirect HTTP — e o nosso `url_fetcher` recusa TUDO que nao seja
# `data:`, entao nao existe requisicao HTTP pra redirecionar. A falha nao e
# alcancavel nesta configuracao.
# 🪤 O salto seria de 8 versoes maiores, e o weasyprint NAO esta instalado na
# maquina onde a bancada e escrita: subir seria trocar uma falha fechada por
# uma mudanca de comportamento que ninguem consegue testar antes do deploy.
# ⏭️ Reavaliar quando houver como exercitar a geracao de PDF de verdade.
weasyprint==62.3
pydyf==0.10.0
# 08/09/2026 — 6 falhas (nenhuma ALTA) no 3.1.4. Salto de patch, barato.
# 🪤 O autoescape continua DESLIGADO de proposito nos templates de
# cronograma; quem protege o texto do cliente e o escape no build_context
# mais o url_fetcher de pdf_seguro.py, nao a versao do Jinja.
Jinja2==3.1.6
